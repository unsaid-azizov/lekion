services:
  nginx:
    image: nginx:alpine
    ports:
      - "8090:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - uploads:/data/uploads
    depends_on:
      - frontend
      - backend

  frontend:
    image: ghcr.io/unsaid-azizov/lekion/frontend:latest
    env_file: .env.frontend
    environment:
      - HOSTNAME=0.0.0.0
    expose:
      - "3000"

  backend:
    image: ghcr.io/unsaid-azizov/lekion/backend:latest
    env_file: .env.backend
    expose:
      - "8000"
    volumes:
      - uploads:/data/uploads
    depends_on:
      - postgres
      - redis

  postgres:
    image: postgres:16-alpine
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: lekion
      POSTGRES_USER: lekion
      POSTGRES_PASSWORD: ${DB_PASSWORD:-lekion}
    expose:
      - "5432"

  redis:
    image: redis:7-alpine
    volumes:
      - redisdata:/data
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    expose:
      - "6379"

volumes:
  pgdata:
  redisdata:
  uploads:
